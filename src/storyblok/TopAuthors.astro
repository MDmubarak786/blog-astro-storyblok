---
import { storyblokEditable } from '@storyblok/astro'
 
 const { blok } = Astro.props

 import RichTextRenderer from "@storyblok/astro/RichTextRenderer.astro";
import Logo from './logo.astro';

 ---
  
 <div {...storyblokEditable(blok)} >
   <div class='w-full text-center'>
        <div class="flex flex-row">
          <img src={blok.Profile.filename} class=" w-20 h-20 rounded-full"/>
          <div class=" px-4 flex flex-col gap-1">
            <div  class=" flex text-left">{blok.Name}  </div>
            <p class=" text-[12px] text-[#666666]">{blok.authorQualifications}</p>
            <ul class="flex flex-row gap-x-2 justify-center">
            {
      blok.socialIcons?.map((blok) => {
        return <Logo blok={blok} />
      })
    }
  </ul>
          </div>
          
        </div>
    </div>

 </div>