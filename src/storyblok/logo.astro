---
import { storyblokEditable } from '@storyblok/astro';

const { blok } = Astro.props;
---

<div {...storyblokEditable(blok)}>
  <div class='border border-[#A3A7A8]'>
    <a href={blok.logoLink}>
      <img src={blok.Logoimage.filename} class='w-6 p-1 h-6' /></a
    >
  </div>
</div>
